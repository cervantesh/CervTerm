name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write
  attestations: write


jobs:
  windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: cmd
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Maturity gates
        run: go run ./scripts/check-maturity-gates.go
      - name: Generate Windows resources
        run: |
          go install github.com/josephspurrier/goversioninfo/cmd/goversioninfo@latest
          go run ./scripts/generate-windows-resource.go -arch amd64
      - name: Build Windows package
        run: |
          go run ./scripts/package-beta.go -version ${{ github.ref_name }} -outdir dist
      - name: Authenticode sign Windows executable
        env:
          WINDOWS_CODESIGN_PFX_BASE64: ${{ secrets.WINDOWS_CODESIGN_PFX_BASE64 }}
          WINDOWS_CODESIGN_PASSWORD: ${{ secrets.WINDOWS_CODESIGN_PASSWORD }}
        run: |
          if "%WINDOWS_CODESIGN_PFX_BASE64%"=="" (
            echo Code-signing secrets are not configured; skipping Authenticode signing.
            exit /b 0
          )
          if "%WINDOWS_CODESIGN_PASSWORD%"=="" (
            echo Code-signing secrets are not configured; skipping Authenticode signing.
            exit /b 0
          )
          go run ./scripts/sign-windows-exe.go -exe dist\cervterm-${{ github.ref_name }}-windows\cervterm.exe
      - name: Package signed Windows zip
        run: |
          go run ./scripts/package-beta.go -version ${{ github.ref_name }} -outdir dist -reuse
      - name: Release preflight (Windows package)
        run: |
          go run ./scripts/release-preflight.go -version ${{ github.ref_name }} -outdir dist -windows-zip dist\cervterm-${{ github.ref_name }}-windows.zip
      - name: Installed package smoke
        run: |
          go run ./scripts/smoke-installed-package.go -zip dist\cervterm-${{ github.ref_name }}-windows.zip -expected-version ${{ github.ref_name }}
      - name: Daily-driver smoke
        run: |
          go run ./scripts/daily-driver-smoke.go -cervterm dist\cervterm-${{ github.ref_name }}-windows\cervterm.exe -workdir dist\daily-driver-smoke
      - uses: actions/upload-artifact@v7
        with:
          name: cervterm-windows-amd64
          path: |
            dist\*.zip
            dist\daily-driver-smoke\*

  linux-headless:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: Maturity gates
        run: go run ./scripts/check-maturity-gates.go
      - name: Build Linux headless zip
        run: |
          version="${{ github.ref_name }}"
          mkdir -p "dist/cervterm-$version-linux-headless-amd64"
          go build -ldflags "-X cervterm/internal/buildinfo.Version=$version" -o "dist/cervterm-$version-linux-headless-amd64/cervterm-linux-amd64" ./cmd/cervterm
          "dist/cervterm-$version-linux-headless-amd64/cervterm-linux-amd64" --print-default-config > "dist/cervterm-$version-linux-headless-amd64/cervterm.lua"
          cp README.md CHANGELOG.md SUPPORT.md "dist/cervterm-$version-linux-headless-amd64/"
          cp -R docs "dist/cervterm-$version-linux-headless-amd64/docs"
          python -m zipfile -c "dist/cervterm-$version-linux-headless-amd64.zip" "dist/cervterm-$version-linux-headless-amd64"
      - uses: actions/upload-artifact@v7
        with:
          name: cervterm-linux-headless-amd64
          path: dist/*.zip

  publish:
    runs-on: ubuntu-latest
    needs:
      - windows
      - linux-headless
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: cervterm-*
          merge-multiple: true
      - name: Generate checksums
        run: |
          cd dist
          sha256sum *.zip > SHA256SUMS.txt
          cat SHA256SUMS.txt
      - uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            dist/*.zip
            dist/SHA256SUMS.txt
      - uses: softprops/action-gh-release@v3
        with:
          files: |
            dist/*.zip
            dist/SHA256SUMS.txt
          generate_release_notes: true
